# Madeleine — locked at scaffold 2026-08-17 (versions pinned deliberately;
# do not inherit drift from sibling repos)
fastapi>=0.115
uvicorn>=0.32
psycopg[binary]>=3.1
python-dotenv>=1.0
APScheduler>=3.10
httpx>=0.27
anthropic>=0.40
pgvector>=0.3
# Sprint 1+ (embeddings — local bge-m3; torch install is CUDA cu126 wheel):
sentence-transformers>=3.0
# Phase 5 (deep flavor reader):
transformers>=4.44
accelerate>=0.33
